<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trackhealth"
        tools:targetApi="31">
        <activity
            android:name=".Register_process"
            android:theme="@style/Theme.Register_process"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddPatientPage"
            android:exported="false" />
        <activity
            android:name=".HomePage"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="false"
            android:theme="@style/Theme.HomePage" />
        <activity
            android:name=".DescriptionPage"
            android:exported="false"
            android:theme="@style/Theme.StartFragment" />
        <activity
            android:name=".RegisterPage"
            android:exported="false"
            android:theme="@style/Theme.RegisterPage" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.Loginpage"
            android:exported="false"/>
    </application>

</manifest>